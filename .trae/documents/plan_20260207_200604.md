我将修改 `Write.tsx` 文件，将 `DEFAULT_TAGS` 常量设置为空数组。

**具体修改：**

*   **文件**：`client/src/pages/Write.tsx`
*   **操作**：将 `const DEFAULT_TAGS = ['技术', '生活', '随笔', '分享'];` 修改为 `const DEFAULT_TAGS: string[] = [];`。

**效果：**
1.  不再有“默认标签”的概念，所有标签都被视为普通标签。
2.  用户可以删除下拉列表中此前被锁定为“默认”的标签（如“技术”、“生活”等）。
3.  满足“默认为空”的需求，即没有预设的、不可删除的标签。

此修改仅涉及前端逻辑，不影响后端数据。如果后端 `GET /users/tags` 仍然返回这些标签，它们将作为普通标签显示，并且现在可以被用户从推荐列表中删除（仅前端删除，除非有对应的后端删除接口调用，目前代码逻辑是 `handleDeleteAvailableTag` 仅更新本地状态 `availableTags`）。

确认无误后，我将执行此更改。