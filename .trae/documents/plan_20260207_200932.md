我将更新 `client/src/pages/Admin/PostManagement.tsx` 以同步 `Write.tsx` 中的功能。

**计划步骤：**

1.  **引入必要的依赖和类型定义**：
    *   添加 `Category` 接口。
    *   更新 `Post` 接口或 `editForm` 状态以包含 `category_id` 和 `tags`。
    *   引入 `useRef` 等 Hook。

2.  **状态管理更新**：
    *   在 `PostManagement` 组件中添加 `categories` (分类列表) 和 `availableTags` (可用标签列表) 状态。
    *   添加用于控制标签和分类下拉框显示的状态 (`showTagDropdown`, `showCategoryDropdown`) 和定时器 Ref。
    *   添加 `DEFAULT_TAGS` 常量并设为空数组 `[]`。
    *   在组件挂载时（`useEffect`）获取分类和标签列表。

3.  **编辑功能增强**：
    *   更新 `handleEditClick`：在获取文章详情后，设置 `editForm` 的 `category` 和 `tags` 字段。
    *   更新 `handleUpdate`：在提交更新时，包含 `category` 和 `tags` 数据。同时更新后端标签库（`api.post('/users/tags', ...)`）。

4.  **UI 重构 (同步 Write.tsx)**：
    *   **移植辅助函数**：复制 `getTagColor` 等辅助函数。
    *   **重构编辑模态框**：
        *   添加分类选择器：使用 `Write.tsx` 中的自定义下拉组件样式和逻辑。
        *   添加标签输入器：使用 `Write.tsx` 中的自定义标签输入组件样式和逻辑（支持回车添加、删除、下拉选择）。
        *   确保样式（`input-field`, `colors` 等）与 `Write.tsx` 一致。

5.  **交互逻辑移植**：
    *   复制并调整 `handleCategoryDropdownEnter/Leave`。
    *   复制并调整 `handleAddTag`, `handleRemoveTag`, `handleDeleteAvailableTag` 等逻辑。

**目标**：使后台管理页面的文章编辑功能拥有与前台创作中心完全一致的分类选择（自定义下拉）和标签管理（输入/选择/删除）体验，且默认标签为空。