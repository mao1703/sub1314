我将优化所有页面的样式，使其能够自适应浏览器窗口大小，解决您提到的 `/write` 页面显示大小错误的问题。

### 计划如下：

1.  **优化 `client/src/pages/Write.tsx`**:
    *   移除固定高度 `calc(100vh - 80px)`，改为 `min-height` 或更灵活的布局。
    *   调整 `padding` 和 `margin`，使其在小屏幕上更紧凑。
    *   使用 Flexbox 或 CSS Grid 实现响应式布局，在小屏幕上将双栏布局调整为单栏（上下排列）。

2.  **优化其他页面**:
    *   **Home.tsx**: 调整文章列表和网格布局，在小屏幕上单列显示。
    *   **PostDetail.tsx**: 调整容器宽度和内边距，确保在移动端阅读体验良好。
    *   **Login.tsx / Register.tsx**: 调整表单容器宽度，在小屏幕上占满宽度。
    *   **Profile.tsx**: 调整卡片样式。

3.  **全局样式调整**:
    *   在 `App.tsx` 或 `index.css` 中设置基础容器的最大宽度和内边距策略。

请确认是否执行此计划？