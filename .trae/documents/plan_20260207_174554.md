我将完善文章详情页功能以满足您的需求。

### 计划如下：

1.  **完善 `client/src/pages/PostDetail.tsx`**：
    *   **引入 `useAuth`**: 用于获取当前登录用户信息，以便判断是否是文章作者。
    *   **权限判断**: 比较 `user.id` 和 `post.author_id`，如果匹配则显示“编辑”和“删除”按钮。
    *   **删除功能**: 实现 `handleDelete` 函数，调用 `api.delete('/posts/:id')`，成功后跳转回首页。
    *   **编辑功能**: 点击“编辑”按钮跳转到 `/edit/:id`（需要先确认是否有此路由，或者复用 `/write` 路由并支持传入 ID 进行编辑）。
        *   *方案*: 为了简化，我建议复用 `Write` 组件，让它支持编辑模式。这里先只实现“跳转到编辑页”的逻辑，稍后如果需要可以进一步完善 `Write` 组件。或者，更简单的做法是先只做删除，编辑功能留待后续完善（虽然您的要求里提到了“编辑”按钮，但没明确要求实现编辑页面的逻辑，不过为了完整性，我会预留编辑入口）。
    *   **详情展示**: 
        *   使用 `react-markdown` 渲染内容。
        *   显示标题、作者、发布时间、浏览次数、分类标签。

2.  **后端接口确认**:
    *   `getPostById` 接口返回的字段中是否包含 `author_id`？
    *   查看后端代码 `postController.js`，查询语句是 `SELECT a.*, u.username ...`，`a.*` 包含了 `author_id`，所以前端可以直接获取到。

### 待确认点（我将按默认最佳实践处理）：
*   编辑路由暂时指向 `/write?id=xxx`，后续可以在 `Write.tsx` 中处理。
*   样式将保持简洁风格。

请确认是否执行此计划？