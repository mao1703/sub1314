我将完善写文章页面以满足您的需求。

### 计划如下：

1.  **完善 `client/src/pages/Write.tsx`**：
    *   **布局重构**: 采用两栏布局，左侧为编辑区，右侧为 Markdown 实时预览区。
    *   **状态管理**: 增加 `category`（分类）、`tags`（标签，使用逗号分隔字符串处理）、`cover`（封面URL）的状态。
    *   **编辑模式支持**: 
        *   使用 `useSearchParams` 获取 URL 中的 `id` 参数。
        *   如果存在 `id`，在组件加载时调用 `api.get('/posts/:id')` 获取文章详情并填充表单。
        *   提交时，根据是否存在 `id` 决定是调用 `api.post`（创建）还是 `api.put`（更新）。
    *   **表单增强**:
        *   添加分类选择下拉框（暂时使用硬编码的分类列表，与首页保持一致）。
        *   添加封面图片 URL 输入框。
        *   添加标签输入框。
    *   **预览功能**: 使用 `react-markdown` 组件在右侧实时渲染 `content`。

2.  **样式优化**: 简单的 Flex 布局实现左右分栏，确保编辑器高度适中。

### 待确认点（我将按默认最佳实践处理）：
*   分类数据暂时硬编码。
*   标签以字符串形式存储在前端（如 "tag1, tag2"），提交时转换为数组。
*   封面目前仅支持输入图片 URL，暂不实现文件上传（因为没有后端上传接口）。

请确认是否执行此计划？