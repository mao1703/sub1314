用户希望增强标签输入功能和提交逻辑，具体要求如下：

1.  **标签容器功能**：
    *   **多选和删除**：目前代码已经实现了 `tags` 数组存储多个标签，并提供删除按钮。
    *   **默认值为空**：目前的初始状态 `useState([])` 已经是空。
    *   **用户手动输入**：目前已经通过 `input` 和 `handleKeyDown` (Enter) 实现了手动输入。
    *   **输入安全检查**：这是新增需求。需要确保输入的标签不包含危险字符（如 HTML 标签、特殊脚本字符等），防止 XSS 注入。
    *   **美化前端页面**：用户希望进一步美化。目前的样式已经使用了 flex 布局和 chip 样式，可能需要调整间距、颜色或交互效果。

2.  **提交按钮逻辑**：
    *   **提交到后端保存**：目前的 `handleSubmit` 已经调用 `api.post` 或 `api.put`。
    *   **前端展示**：保存成功后跳转到详情页展示，这是现有逻辑。
    *   **数据库最多支持10个保留**：这是关键的新增逻辑。用户希望数据库层面的标签（可能是指用户的常用标签库，或者单篇文章的标签数）限制在 10 个。如果超出，需要提示用户删除。
    *   **提示删除**：在提交前或输入时检查标签数量。

**分析与计划：**

1.  **输入安全检查 (`handleAddTag`)**：
    *   添加正则校验，仅允许汉字、字母、数字、下划线、短横线。禁止特殊字符。
    *   限制单个标签长度（例如 20 字符）。
    *   限制标签数量（虽然用户说数据库最多支持 10 个，通常也意味着单篇文章不能超过 10 个，或者用户总共不能存超过 10 个？结合上下文“数据库保留”，可能是指用户的**推荐标签库**。但根据之前的修改，推荐功能已被移除。这里的“数据库最多支持10个保留”可能指**单篇文章**最多关联 10 个标签，或者**用户最近使用的标签**只存 10 个）。
    *   **假设**：用户指的是**单篇文章最多允许添加 10 个标签**。如果超出，前端拦截并提示。

2.  **提交逻辑 (`handleSubmit`)**：
    *   在提交前再次检查标签数量是否超过 10 个。
    *   虽然用户提到“数据库最多支持10个保留，若超出提示删除”，这通常是在提交时后端返回错误，或者前端提前校验。为了用户体验，前端校验更好。

3.  **美化**：
    *   优化 Input 的 focus 样式。
    *   优化 Tag Chip 的渐变或阴影效果。

**具体修改步骤：**

1.  **修改 `handleAddTag`**：
    *   增加数量检查：`if (tags.length >= 10) { alert('最多添加10个标签'); return; }`。
    *   增加格式检查：正则 `/^[a-zA-Z0-9\u4e00-\u9fa5-_]+$/`。
    *   增加长度检查。

2.  **修改 `handleSubmit`**：
    *   保留现有的提交逻辑，但确保在提交前校验标签数量。

3.  **美化 UI**：
    *   微调 `input-field` 的样式，增加 `focus-within` 效果（通过 state 或 CSS）。

**关于“数据库最多支持10个保留”的澄清**：
如果是指“用户的历史标签库”只保留 10 个，这通常是后端逻辑（我们在之前的修改中移除了前端对 `/users/tags` 的同步调用）。如果用户现在的意图是**重新引入**某种同步机制但限制数量，那与之前的“清除所有逻辑”相悖。
结合“若超出提示删除”，更像是**单篇文章的标签数量限制**。
**另一种可能**：用户希望在提交文章时，同时把这些标签存入“用户标签库”，但这个库最多存 10 个。如果用户的标签库满了，提示用户去清理？这有点复杂且打断流程。
**最合理的解释**：单篇文章最多允许 10 个标签。

**确认方案**：
1. 限制单篇文章标签数量 <= 10。
2. 标签输入增加 XSS 过滤/正则校验。
3. 美化输入框。

**Code Reference**: `d:\项目1\11\client\src\pages\Write.tsx`

**Action**: 更新 `handleAddTag` 和组件样式。