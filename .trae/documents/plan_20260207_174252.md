我将完善首页功能以满足您的需求。

### 计划如下：

1.  **完善 `client/src/pages/Home.tsx`**：
    *   **状态管理**: 添加 `keyword`（搜索关键词）、`category`（当前分类）、`page`（当前页码）、`totalPages`（总页数）等状态。
    *   **搜索栏**: 添加输入框和搜索按钮，绑定 `keyword` 状态。
    *   **分类筛选**: 添加顶部分类标签栏（需要先获取分类列表，如果后端没有专门的分类接口，可以先硬编码几个测试分类，或者添加一个获取分类的接口）。
        *   *建议*: 暂时先硬编码一些常见分类（如 "Technology", "Life", "Coding"），或者快速实现一个获取分类的后端接口。考虑到完整性，我将在 `Home.tsx` 中先模拟几个分类标签。
    *   **文章列表**: 优化展示样式，显示标题、封面（如果有）、分类、作者、发布时间。
    *   **分页组件**: 在底部添加分页控制器（上一页、下一页、页码显示）。
    *   **数据获取**: `useEffect` 监听 `page`, `category`, `keyword` 的变化，自动重新请求 `api.get('/posts')` 并更新列表。

2.  **交互优化**：
    *   搜索时重置页码为 1。
    *   切换分类时重置页码为 1。
    *   点击文章标题或封面跳转到 `/post/:id`。

### 待确认点（我将按默认最佳实践处理）：
*   后端目前 `getPosts` 接口已经支持 `page`, `pageSize`, `category`, `keyword` 参数，无需修改后端。
*   前端样式将使用简单的内联样式或 CSS，保持整洁。

请确认是否执行此计划？